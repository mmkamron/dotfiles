local options = {
    ensure_installed = { "rust" , "python", "go", "typescript", "javascript", "lua", "markdown" },
    highlight = { enable = true },
    additional_vim_regex_highlighting = false,
    indent = { enable = true }
}

return options
